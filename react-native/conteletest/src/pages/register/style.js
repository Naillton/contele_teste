import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },

  background: {
    width: 340,
    height: 340,
    borderRadius: 20,
    backgroundColor: '#197CEC',
    opacity: 0.7,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
