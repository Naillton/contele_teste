import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btn: {
    alignItems: 'center',
    height: 40,
    width: 140,
    padding: 10,
    margin: 10,
    backgroundColor: '#ffbd2c',
    borderRadius: 10,
  },
});
