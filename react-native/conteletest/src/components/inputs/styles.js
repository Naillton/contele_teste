import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  camps: {
    width: 240,
    height: 60,
    borderWidth: 2,
    borderRadius: 4,
    borderColor: 'black',
    padding: 20,
    margin: 20,
  },
});
